import { Box, Grid } from "@mui/material";

const Footer = () => {
  return (
    <Grid sx={{ display: "flex", justifyContent: "center", pt: 3 }}>
      <Box>hello from footer</Box>
    </Grid>
  );
};

export default Footer;
